// declareren van variabelen, let (veranderbaar) enkel toepasbaar binnen accolades
let bewerking =  "(1 + 2) * 3 + 4 / 2";
let resultaat =  (1 + 2) * 3 + 4 / 2;

// expected output, use toString to resolve Concatenation:
console.log(bewerking+" = "+resultaat.toString());